metadata:
  name: Interconnect_Provider_Validation
  format: "Lava-Test Test Definition 1.0"
  description: "Validate Qualcomm ICC provider discovery, driver binding, synchronization, and kernel health"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/Interconnect_Provider_Validation
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh Interconnect_Provider_Validation.res
