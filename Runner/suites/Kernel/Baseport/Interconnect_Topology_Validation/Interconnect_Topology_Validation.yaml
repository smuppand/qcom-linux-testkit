metadata:
  name: Interconnect_Topology_Validation
  format: "Lava-Test Test Definition 1.0"
  description: "Validate the Linux ICC debugfs node and link topology"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/Interconnect_Topology_Validation
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh Interconnect_Topology_Validation.res
