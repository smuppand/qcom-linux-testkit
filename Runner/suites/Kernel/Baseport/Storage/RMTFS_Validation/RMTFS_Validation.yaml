metadata:
  name: RMTFS_Validation
  format: "Lava-Test Test Definition 1.0"
  description: "Functional RMTFS module, shared-memory, daemon, transport, and kernel-log validation with state restoration."
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/Storage/RMTFS_Validation
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh RMTFS_Validation.res
