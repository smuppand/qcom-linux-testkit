metadata:
  name: UART_Validation
  format: "Lava-Test Test Definition 1.0"
  description: "Validate UART controller, TTY, console, and serdev runtime readiness"
  os:
    - linux
  scope:
    - functional

run:
  steps:
    - REPO_PATH=$PWD
    - cd Runner/suites/Kernel/Baseport/UART_Validation
    - ./run.sh || true
    - $REPO_PATH/Runner/utils/send-to-lava.sh UART_Validation.res
